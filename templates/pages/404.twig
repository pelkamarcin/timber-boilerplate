{% extends 'layouts/base.twig' %}

{% block content %}
    <div class="c-error-page">
        <div class="c-error-page__content s-textcontent">
            <h1 class="c-error-page__heading">{{ __('Error 404','sfy') }}</h1>
            <p class="c-error-page__message">{{ __('The page you are looking for does not exists or have been removed','sfy') }}</p>
        </div>
    </div>
{% endblock %}

